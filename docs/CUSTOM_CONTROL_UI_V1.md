# Custom Control UI v1 (szmyt)

Scope for first iteration:
- Agent Graph (agent -> subagent tree)
- Channel Accounts view (per-account health)
- Pairing Center (pending/approve/reject)
- Cron runs summary
- Incident timeline (disconnect/reconnect/restart)

Branch: `feature/custom-control-ui`
